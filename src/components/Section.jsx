import React from "react";

const Section = ({
  className = "",
  id = "",
  crosses = false,
  crossesOffset = false,
  customPaddings = "",
  children = null,
}) => {
  return;
};

export default Section;
